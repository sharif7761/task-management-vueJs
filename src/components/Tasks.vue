<template>
  <div :key="task.id" v-for="task in tasks.tasks"> 
    <h3>{{task.text}}</h3>
  </div>
</template>

<script>
export default {
  name: 'TaskList',
  props: {
    tasks: Array,
  }
}
</script>